<%# Start non-card card %>
<div class="d-flex justify-content-center">
  <div class="card-md">

    <%# NOTE: this is WIP - check and see if it works... it might need a row-col wrapper or something around the header and body... %>
    <%# DeleteThisPls - insert card header %>

    <%# DeleteThisPls - insert card body option %>

  <%# End card %>
  </div>
</div>
