// render flash.
$("#flash").html("<%= escape_javascript(render('layouts/flash')) %>");

// ChamgeThisPls
$("#CopyIdFromView").html("<%= escape_javascript(render(partial: 'CopyPathFromView', locals: { ChangeLocals })) %>");

// DeleteThis - if coming from modal, make sure to manually close modal 

// DeleteThis - include the following if coming from show edit swap 
// Restore links and buttons 
restoreLinksAndButtons();
