<%# REVIEW: the below and delete this section when done %> 
resources - plural name of the model - ex. 'users'
item - name of the model - ex. 'user' (ask manager if you are not sure)
adjective-model-<%= model.id %>-nouns - name of the identifier (ask manager if you are not sure)
adjective-model-new-nouns - name of the identifier for new (ask manager if you are not sure)

<%# gray / white alternating %> 
<% resources.count == 1 ? row = 1 : row = 0 %>
<% resources.each do |model| %>

  <%# begin card body wrapper %>
  <div class="row mx-2" style="background-color: <%= "#f5f5f5" if row.even? %>;">
  <%# NOTE: if not using gray / white, delete  style %>
    <% row += 1 %> 
    <div class="col-12 px-2">

      <div id="show-adjective-model-<%= model.id %>-nouns" class="adjective-model-<%= model.id %>-nouns-show-element"> 
        <%= render(partial: 'app/section/middle_path/final_controller/show', locals: { model: model }) %> 
        <%# REVIEW: update path to partial and locals %>
        <%# REVIEW: create actual partial if you have not already %>
      </div>

      <%# Edit Partial With Errors. Needs to be on top in case there are check boxes %>
      <div id="edit-adjective-model-<%= model.id %>-nouns-with-errors" class="adjective-model-<%= model.id %>-nouns-edit-element" style="display: none;">
      </div>

      <%# Edit Partial %>
      <div id="edit-adjective-model-<%= model.id %>-nouns" class="adjective-model-<%= model.id %>-nouns-edit-element" style="display: none;">
        <%= render(partial: 'app/section/middle_path/final_controller/edit', locals: { model: model }) %> 
        <%# REVIEW: update path to partial and locals %>
        <%# REVIEW: create actual partial if you have not already %>
      </div>

      <%# NOTE: sub add can be added here with model.some_list %>
      <%# NOTE: Most sub adds you will want to remove the mx-2 and px-2 on any of the "begin card body wrapper"s %>
      <%# NOTE: id for new container, class for new container, id for new with errors container, class for new with errors container, classes on add button, onclick on add button and cancel on new form on sub will all need to have parent model id because otherwise there will be many edit-adjective-model-new-nouns ids  %>

    <%# end card body wrapper %>
    </div>
  </div>
<% end %>

<%# begin card body wrapper %>
<div class="row mx-2" style="background-color: <%= "#f5f5f5" if row.even? %>;">
  <div class="col-12 px-2">
    <%# New Partial With Errors. Needs to be on top in case there are check boxes %>
    <div id="edit-adjective-model-new-nouns-with-errors" class="adjective-model-new-nouns-edit-element" style="display: none;">
    </div>

    <%# Edit Partial %>
    <div id="edit-adjective-model-new-nouns" class="adjective-model-new-nouns-edit-element" style="display: none;">
      <%= render(partial: 'app/section/middle_path/final_controller/new', locals: { model: model }) %> 
      <%# REVIEW: update path to partial and locals %>
      <%# REVIEW: create actual partial if you have not already %>
    </div>
    <%# Add new "button" %>
    <a class="c-pointer text-primary adjective-model-new-nouns-show-element"> 
      <script>
        $('.adjective-model-new-nouns-edit-button').click(function() {
          displayEditForm('adjective-fodel-new-nouns')
        }) 
      </script>
      Add item
    </a>
    <%# end card body wrapper %>
  </div>
</div>
