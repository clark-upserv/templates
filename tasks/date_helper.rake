  # DeleteThis - only run on a specific weekday (Monday = 1)
  if Date.today.wday == 1
    # DeleteThis - insert ruby code
  end
  # DeleteThis - run on multiple specific weekdays (Monday = 1)
  if [1, 2, 3, 4, 5, 6, 7].include?(Date.today.wday)
    # DeleteThis - insert ruby code
  end
  # DeleteThis - run on a specific day of the month
  if Date.today.mday == 1
    # DeleteThis - insert ruby code
  end
  # DeleteThis - run on multiple specific days of the month
  if [1, 15].include?(Date.today.mday)
    # DeleteThis - insert ruby code
  end
