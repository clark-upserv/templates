<%# DeleteThis: goal for name on each input: some_models[some_models_list][][some_attribute] %>
<%= ChangeParentForm_form.fields_for('ChangeModels[]ChangeModel') do |ChangeModels_form| %>
  <% ChangeModels.each do |ChangeModel| %>
    <%# DeleteThis: make sure to include propper wrappers (left label, top label, form rows, form groups, etc.) %>
    <%# DeleteThis: if using checkbox - use tag to prevent rails hidden checkbox %>
    <div class="custom-control custom-checkbox">
      <%= check_box_tag(ChangeModels_form.object_name + 'ChangeAttribute', ChangeModel.ChangeAttribute, false, id: "ChangeModel_#{ChangeModel.id}_ChangeAttribute", class: 'custom-control-input') %>
      <%= ChangeModels_form.label(:id, ChangeModel.ChangeAttribute, for: "ChangeModel_#{ChangeModel.id}_ChangeAttribute", class: "custom-control-label text-wrap") %>
    </div>
    <%# DeleteThis: if using other input type %>
    <%= ChangeModels_form.label(:ChangeAttribute, 'ChangeAttribute', for: "ChangeModel_#{ChangeModel.id}_ChangeAttribute") %>
    <%= ChangeModels_form.text_field(:ChangeAttribute, value: ChangeModel.ChangeAttribute, id: "ChangeModel_#{ChangeModel.id}_ChangeAttribute", class: 'form-control') %>
  <% end %>
<% end %>
